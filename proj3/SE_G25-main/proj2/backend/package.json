{
  "name": "food-del-backend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "server": "nodemon server.js",
    "lint": "eslint . --ext .js",
    
    "_comment": "Run everything except the converted modern files",
    "test:legacy": "node --experimental-vm-modules ./node_modules/.bin/jest --testPathIgnorePatterns=bulkFeature.test.js --testPathIgnorePatterns=orderController.test.js",
    
    "_comment2": "Run only the modern files with isolated config",
    "test:modern": "node --experimental-vm-modules ./node_modules/.bin/jest --config jest.bulk.config.mjs",
    
    "_comment3": "The master command that CI/GitHub runs",
    "test": "npm run test:legacy && npm run test:modern",
    
    "test:legacy:coverage": "node --experimental-vm-modules ./node_modules/.bin/jest --testPathIgnorePatterns=bulkFeature.test.js --testPathIgnorePatterns=orderController.test.js --coverage --coverageReporters=lcov --coverageReporters=text",
    
    "test:modern:coverage": "node --experimental-vm-modules ./node_modules/.bin/jest --config jest.bulk.config.mjs --coverage --coverageReporters=lcov --coverageReporters=text",
    
    "test:coverage": "npm run test:legacy:coverage && npm run test:modern:coverage",
    
    "test:preferences": "node --experimental-vm-modules ./node_modules/.bin/jest --testMatch='**/__tests__/preferences.test.js'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.1.1",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.8.1",
    "stripe": "^14.17.0",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "cross-env": "^10.1.0",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.11",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {},
    "moduleFileExtensions": [
      "js",
      "json",
      "mjs"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ],
    "setupFilesAfterEnv": [
      "<rootDir>/jest.setup.mjs"
    ],
    "collectCoverageFrom": [
      "controllers/**/*.js",
      "models/**/*.js",
      "middleware/**/*.js",
      "routes/**/*.js"
    ],
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/__tests__/"
    ],
    "coverageReporters": [
      "lcov",
      "text",
      "html"
    ],
    "testTimeout": 30000
  }
}
